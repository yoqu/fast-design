{
  "name": "fast-design",
  "private": true,
  "version": "0.1.0",
  "description": "AI product-prototyping tool: describe the product you want and an AI agent builds a clickable, high-fidelity prototype in your browser, with live preview and multi-format export.",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"pnpm --filter server dev\" \"pnpm --filter web dev\"",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
