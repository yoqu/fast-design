{
  "name": "fast-design",
  "private": true,
  "version": "0.1.0",
  "description": "Chat with an AI agent in your browser to design and build web pages, with a live preview workspace and multi-format export.",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"pnpm --filter server dev\" \"pnpm --filter web dev\"",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
